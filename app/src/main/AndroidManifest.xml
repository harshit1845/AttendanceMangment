<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Attendance Managment"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".STUDENT2"></activity>
        <activity android:name=".STUDENT" />
        <activity android:name=".TEACHER2" />
        <activity android:name=".TEACHER" />
        <activity android:name=".ADMIN2" />
        <activity android:name=".ADMIN" />
        <activity android:name=".SIR" />
        <activity android:name=".FIRST" />
        <activity android:name=".LOGIN">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".REG" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>